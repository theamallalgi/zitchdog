To use any of these themes in Ghostty, just clone the Zitchdog repo
and set the theme to the absolute path of the theme file.

Example:

```ini
theme = "/home/user/projects/zitchdog/extras/ghostty/zitchdog-grape"
```